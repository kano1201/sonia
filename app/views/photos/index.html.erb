<div class="container">
  <div class="row mt-5 mb-5">
    <div class="col-xs-12 col-lg-8 col-xl-9 mx-auto">
        <h5 class="mt-5 mt-lg-0 text-center">New Posts</h5>
        <%= render 'index', photos: @photos %>
    </div>
  </div>
  <%= paginate @photos %>
</div>